<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.yeapin.androidproject.SingleTopModeActivity" >

    <Button
        android:id="@+id/singleSelfBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" 
        android:text="跳转至SingleTopModeActivity"/>

    <Button
        android:id="@+id/singleModeBtn"
        android:layout_below="@id/singleSelfBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="跳转至WorldActivity" />

    <TextView
        android:id="@+id/singleModeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/singleModeBtn"
        android:text="@string/hello_world" />

</RelativeLayout>